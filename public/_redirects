# Cloudflare Pages SPA fallback: serve index.html for any path that isn't a real file.
# Existing static assets (js/css/sw.js/manifest) take precedence, so this only catches app routes.
/*    /index.html   200
