node_modules/
dist/
.env
.env.*
*.local
.dev.vars
.DS_Store
Thumbs.db
.vite/
.wrangler/

# Internal working/handoff docs — keep local, never publish to the open-source repo.
/COWORK-HANDOFF.md
/SHERLOCK-DEMO-HANDOFF.md
/READING-REFERENCES-HANDOFF.md
/SHERLOCK-REFERENCE-HANDOFF.md

# TJ's private Cosmos content — used locally as dev data, NEVER shipped in the public repo.
# Regenerate locally with `npm run build:sample` (reads C:\Projects\story-workbench).
# The public app's example world will be a separate, neutral demo (decided later).
/src/lib/sample/

# build-public.mjs parks the private sample here mid-build; never commit
/.sample-hidden/
# quarantine for files the device bridge cannot delete
/_to_delete/
